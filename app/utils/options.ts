export const takeImageOptions: any = {
  saveToPhotos: true,
  mediaType: 'photo',
  includeBase64: false,
  includeExtra: true,
};

export const chooseImageOptions: any = {
  maxHeight: 200,
  maxWidth: 200,
  selectionLimit: 0,
  mediaType: 'photo',
  includeBase64: false,
  includeExtra: true,
};